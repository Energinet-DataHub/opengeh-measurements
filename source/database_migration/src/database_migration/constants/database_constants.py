class DatabaseConstants:
    # Should probably be an environment variable because it is created in Terraform.
    bronze_database = "measurements_bronze"
    migrations_database = "measurements_internal"
