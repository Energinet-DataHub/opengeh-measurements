# subsystem_tests:
# A DataBricks token is required when running container tests locally. (dbw-workspace-token)
DATABRICKS_TOKEN=<databricks_token>

# The Databricks workspace URL is required when running container tests locally. (dbw-workspace-url)
WORKSPACE_URL=<workspace-url>